map $http_origin $cors_origin {
    default "";
    "https://maps.minersonline.uk"     $http_origin;
    "https://maphost.minersonline.uk"  $http_origin;
}

server {
    listen 80;
    server_name maphost.minersonline.uk;

    # --- Proxy Minecraft map ---
    location /minecraft/smp1/ {
        # --- CORS preflight ---
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Origin, Content-Type, Accept" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }

        proxy_pass http://desktop-0uc53li.netbird.cloud:8080/;

        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_redirect off;

        add_header Access-Control-Allow-Origin $cors_origin always;
        add_header Access-Control-Allow-Credentials "true" always;
    }

    # Optional: block everything else explicitly
    location / {
        return 404;
    }
}
